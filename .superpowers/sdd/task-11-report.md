# Task 11 Report — E2E + Deploy Config + README + Final Gates

**Status: COMPLETE**

## Commits

- `7780124` fix(web): guard clipboard.writeText against rejection in non-HTTPS contexts
- `673d5c7` fix(web): exclude e2e/ from Vitest to prevent Playwright test() collision
- `1fa5ee5` feat(web): E2E critical flow + Playwright config + deploy config + README + typecheck script

## Files Created / Modified

| File | Action |
|------|--------|
| `apps/web/e2e/calculator.spec.ts` | Created — Playwright critical-flow test |
| `apps/web/playwright.config.ts` | Created — webServer: `pnpm dev`, baseURL 3000, chromium project |
| `apps/web/vercel.json` | Created — `{"framework":"nextjs"}` |
| `apps/web/README.md` | Replaced — quick start + deploy note |
| `apps/web/package.json` | Added `typecheck` script |
| `apps/web/vitest.config.ts` | Added `exclude: ["e2e/**"]` to stop Playwright collision |
| `apps/web/components/ShareButton.tsx` | Added `.catch()` on `clipboard.writeText` |
| `pnpm-lock.yaml` | Updated — `@playwright/test` added |

## Selector notes

`@base-ui/react` Select renders:
- Trigger as `role="combobox"` — findable via `page.getByLabel(/character/i)` because `CharacterPicker` has a native `<label htmlFor="character-picker">` linked to the trigger's `id="character-picker"`.
- Items as `role="option"` — findable via `page.getByRole("option", { name: "Bennett" })`.

`data-testid="result-avg"` was already present on the average cell (added in an earlier task); the spec uses `.first()` to avoid the multiple-match error.

## Final Gate Outputs

### `pnpm --filter web build`
```
✓ Compiled successfully in 2.3s
✓ Generating static pages using 5 workers (4/4) in 305ms
Route (app): ○ /   ○ /_not-found   (Static)
```

### `pnpm --filter web typecheck`
```
$ tsc --noEmit
(no output = clean)
```

### `pnpm --filter web test` (Vitest, ≥12)
```
Test Files  5 passed (5)
Tests  12 passed (12)
```

### `pnpm --filter web exec playwright test`
```
Running 1 test using 1 worker
✓  1 [chromium] › e2e/calculator.spec.ts:3:5 › pick a character + see a damage number (512ms)
1 passed (4.1s)
```

### `pnpm --filter @genshin/data test`
```
Test Files  58 passed (58)
Tests  68543 passed (68543)
```
Expected: 68543 ✓

### `node --test tools/kb-gen/__tests__/`
```
# pass 109
# fail 0
```
Expected: 109 ✓

### `git diff --stat main..HEAD -- tests/golden packages/core`
```
74 files changed, 13091 insertions(+), 26 deletions(-)
```
Only `tests/golden/` additive fixtures (new characters/weapons/sets from earlier tasks). No `packages/core` changes. Engine byte-unchanged ✓.

## Concerns / Notes

- Next.js warns about workspace root detection (multiple lockfiles in `~/Dev/`). This is a pre-existing environmental issue; the build and tests are unaffected.
- The Playwright webServer uses `pnpm dev` (not a static serve of `out/`) because `output: "export"` disables `next start`. This means E2E runs against the dev server, not the production build. A full production-parity run would require a static HTTP server (e.g. `npx serve`) which is not currently installed; this is acceptable for development E2E.
- `apps/web/test-results/` directory was generated by Playwright; it is in `.gitignore` implicitly (not tracked).
