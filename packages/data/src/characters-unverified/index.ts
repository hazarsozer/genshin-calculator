/** Transient staging barrel — past-v5.8 chars being gated vs GCSim. NOT globbed by the vitest suites
 * (they glob characters/*.ts). The gate's _port-entry.ts merges this for resolution. */
export { flins } from "./flins.js";
